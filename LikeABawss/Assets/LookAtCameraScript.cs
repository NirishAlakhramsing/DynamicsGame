using UnityEngine;
using System.Collections;

public class LookAtCameraScript : MonoBehaviour {

	// Use this for initialization
	void Start () {
        
    }
	
	// Update is called once per frame
	void Update () {
        
    }

    //When hit show the health bar minus the damage and fade off again
    void HealthbarChange()
    {
        //First stop all other animations on this.

        //Set alpha back to full

        //Remove a heart

        //Start fading out again
    }
}
